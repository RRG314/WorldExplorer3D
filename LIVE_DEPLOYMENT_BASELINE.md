# Live Deployment Baseline

This workspace was created as a clean baseline for the version currently served at:

- https://worldexplorer3d.io
- Firebase project/site: `worldexplorer3d-d9b83`
- Firebase Hosting live version: `f94339c175099f51`
- Release time: `2026-03-20T14:39:06.499Z`
- Release tool: Firebase CLI
- Released by: `Sreid1118@gmail.com`
- Hosting file count: `183`

The live deploy evidence came from Firebase Hosting channel metadata and the live site headers. The local Firebase cache that matches this deploy timestamp is in:

`/Users/stevenreid/Documents/New project/.firebase/hosting.cHVibGlj.cache`

The active live files were downloaded into `public/` from `https://worldexplorer3d.io` using the Firebase Hosting manifest saved in:

`LIVE_FIREBASE_HOSTING_VERSION.json`

Editable root mirrors were aligned from `public/` for the normal project folders:

- `index.html`
- `about/`
- `account/`
- `app/`
- `assets/`
- `js/`
- `legal/`

Firebase deploy config and function source were copied from:

`/Users/stevenreid/Documents/New project drive freeze live`

That worktree's `firebase.json` matches the live Hosting rewrite table for the March 20 deploy, including the contribution, overlay, admin dashboard, billing, and legal routes.

Recommended working rule: use this folder as the baseline for fixes intended to land on the currently deployed site. Avoid editing the older scattered worktrees unless you are intentionally mining them for prior experiments.
