# Global transport enclosure research and implementation

2026-09-07. Local work only. Audience: World Explorer owner and maintainers.

The plan tool is not available in this session; this file records the plan.

Scope: global data/topology, tunnel junction enclosure, portal/terrain interfaces,
bridge transition continuity, shared collision/camera ownership. Preserve mapped
buildings, ordinary roads, and player controls. No deployment or location fixes.
No source can establish surveyed global geometry from generalized cartography.

1. Discovery — complete: primary OSM/Shortbread, ASAM, geometry-library, terrain,
   and scene-query documentation; inspect the corresponding current code.
2. Follow-up and synthesis — complete: reconcile source capability limits,
   compare closed-solid union with 2.5D patches, prototype bounded compilation.
3. Implement selected shared boundary and vehicle-camera protection — checkpoint implemented.
4. Verification and remaining integration — in progress. 31 focused checks and
   source graph pass; Monaco fallback and Fort McHenry gameplay inspected.
   Exact-provider, broader bridge coverage, portal fit, full artifact and actual
   phone gates remain open. Do not replace these with structural flags.
5. Cited research brief created and rendered; checkpoint notes retain unfinished
   acceptance. Local commits preserve progress without declaring global completion.

Evidence gaps: display labels are incorrectly topology inputs (confirmed in code);
Shortbread covered indication lacks tunnel subtype/layer guarantees (schema);
interval deletion cannot join 3D arches (geometry/manuals and screenshots);
robust union performance and source-input validity require an actual prototype.
Research search expansion stops here: these engineering gaps need execution,
not another broad search.
